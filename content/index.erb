---
title:       Home P
created_at:  2008-08-02 14:06:40.000000 -06:00
filter:
  - erb
  - textile
---
<% pages = @pages.find(:limit => :all,
                        :sort_by => 'created_at').reverse
    first = pages.shift %>
<%= pages.size %>
 <h2 id="first"><a href="<%= first.url %>"><%= first.title %><%= first.created_at %></a></h2>
 <% pages.each do |page| %>
   <h2><a href="<%= page.url %>"><%= page.title %></a></h2>
   <%= render :partial => 'tags' %>
   
 <% end %>
